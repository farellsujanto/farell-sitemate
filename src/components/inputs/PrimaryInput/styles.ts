import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    primaryInput: {
        height: 40,
        width: '100%',
        padding: 10,
        borderWidth: 1,
        borderRadius: 12,
    },
});
