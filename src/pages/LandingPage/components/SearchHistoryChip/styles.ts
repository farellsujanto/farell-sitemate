import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        padding: 8,
        borderRadius: 16,
        backgroundColor: '#f0f0f0',
    },
    historyText: {
        fontSize: 10,
        height: 20,
        color: '#303030',
    },
});